#!/usr/bin/env bash
set -euo pipefail
\n+# Continue Strong LoRA training from an existing checkpoint with a smaller LR.\n+#\n+# Why:\n+# - `--resume` restores optimizer state (including LR) from the checkpoint.\n+# - Use `--override_lr_on_resume` to intentionally drop LR for extra fine-tuning.\n+\n+export HF_ENDPOINT=https://hf-mirror.com\n+export PYTHONUNBUFFERED=1\n+\n+SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\n+REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\"\n+cd \"${REPO_ROOT}\"\n+\n+MODEL_NAME=\"depth-anything/DA3-GIANT-1.1\"\n+OUTPUT_DIR=./checkpoints/da3_strong_lora/scannetpp\n+\n+# Resume from the best epoch you observed (default: epoch_4 from a 5-epoch run).\n+RESUME_CKPT=\"${1:-${OUTPUT_DIR}/epoch_4.pt}\"\n+\n+# Add N more epochs by setting TOTAL_EPOCHS > checkpoint_epoch+1.\n+# For example: resuming from epoch_4 (0-indexed) and setting TOTAL_EPOCHS=8 adds 3 more epochs.\n+TOTAL_EPOCHS=\"${2:-8}\"\n+\n+# Smaller LR for continued fine-tuning.\n+LR=\"${3:-1e-5}\"\n+\n+LORA_RANK=64\n+LORA_ALPHA=64\n+NUM_VIEWS=8\n+\n+# Loss weights (keep consistent with the run you're continuing).\n+KL_WEIGHT=1.0\n+COS_WEIGHT=2.0\n+RKD_WEIGHT=2.0\n+RKD_TOPK=4\n+RKD_NUM_REF=256\n+RKD_NUM_SHARED=256\n+\n+OUTPUT_WEIGHT=0.0\n+CAMERA_WEIGHT=5.0\n+DEPTH_WEIGHT=3.0\n+DEPTH_GRAD=grad\n+DEPTH_VALID_RANGE=0.98\n+\n+python -u ./scripts/train_distill.py \\\n+  --dataset scannetpp \\\n+  --use_scannetpp_test_split \\\n+  --samples_per_scene 5 \\\n+  --seeds_list 30 31 32 33 34 \\\n+  --model_name \"${MODEL_NAME}\" \\\n+  --num_views ${NUM_VIEWS} \\\n+  --combined_loss \\\n+  --all_token_softmax_kl_cosine \\\n+  --all_token_softmax_kl_weight ${KL_WEIGHT} \\\n+  --all_token_softmax_cos_weight ${COS_WEIGHT} \\\n+  --distill_all_layers \\\n+  --rkd_weight ${RKD_WEIGHT} \\\n+  --rkd_topk ${RKD_TOPK} \\\n+  --rkd_num_ref_samples ${RKD_NUM_REF} \\\n+  --rkd_num_shared_samples ${RKD_NUM_SHARED} \\\n+  --rkd_angle1_weight 1.0 \\\n+  --rkd_angle2_weight 1.0 \\\n+  --rkd_angle3_weight 1.0 \\\n+  --output_weight ${OUTPUT_WEIGHT} \\\n+  --camera_weight ${CAMERA_WEIGHT} \\\n+  --depth_weight ${DEPTH_WEIGHT} \\\n+  --depth_gradient_loss ${DEPTH_GRAD} \\\n+  --depth_valid_range ${DEPTH_VALID_RANGE} \\\n+  --epochs ${TOTAL_EPOCHS} \\\n+  --batch_size 1 \\\n+  --num_workers 2 \\\n+  --lr ${LR} \\\n+  --override_lr_on_resume \\\n+  --lora_rank ${LORA_RANK} \\\n+  --lora_alpha ${LORA_ALPHA} \\\n+  --lr_scheduler none \\\n+  --weight_decay 1e-5 \\\n+  --resample_per_epoch \\\n+  --resume \"${RESUME_CKPT}\" \\\n+  --output_dir \"${OUTPUT_DIR}\"\n+\n+echo \"Done continuing training. Check: ${OUTPUT_DIR}/epoch_*.pt and *_lora.pt\"\n*** End PatchResponse code: 0
